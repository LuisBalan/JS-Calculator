const digit = document.querySelectorAll('number-key');
const operator = document.querySelector('operator-key');
const equals = document.querySelector('equals-key');
const clear = document.getElementById('clear-key')

