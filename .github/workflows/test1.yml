name: Hello World

on: [push, pull_request]

jobs:
    hola:
        runs-on: ubuntu-latest

        steps:
        - name: Hello World!
          run: echo "Hello World!"

        - name: touch file
          run:  touch hola.txt

        - name: Set text
          run: echo "Hello World!" >> hola.txt

        - name: Read File
          run: cat hola.txt

    other:
        runs-on: macos-12
        needs: [hola]

        steps:
        - name: LS!
          run: ls -lr